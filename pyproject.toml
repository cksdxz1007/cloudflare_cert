[project]
name = "cloudflare-cert-manager"
version = "2.0.0"
description = "Cloudflare Origin CA certificate management tool"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.0",
    "pyyaml>=6.0",
    "questionary>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
